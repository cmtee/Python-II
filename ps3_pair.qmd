---
title: "30538 Problem Set 3: git pair"
author: "Clarice & Eddie"
format: 
  pdf:
    keep-tex: true
    include-in-header: 
       text: |
         \usepackage{fvextra}
         \DefineVerbatimEnvironment{Highlighting}{Verbatim}{breaklines,commandchars=\\\{\}}
include-before-body:
  text: |
    \RecustomVerbatimEnvironment{verbatim}{Verbatim}{
      showspaces = false,
      showtabs = false,
      breaksymbolleft={},
      breaklines
    }
---

```{python}
import pandas as pd
import numpy as np

np.random.seed(42)
data = pd.DataFrame(np.random.rand(100), columns=['Random Numbers'])

def describe(df):
    first_object = "Preview of the DataFrame:"  # First object: header material
    second_object = df.head()  # Second object: first few rows of the DataFrame
    return [first_object, second_object]  # Return a list with both objects

# Example of using the function
result = preview_data(data)
print(result[0])  # This prints the header material
print(result[1])  # This prints the DataFrame preview
```